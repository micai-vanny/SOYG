<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="admin.member.service.member-mapping">

	<!-- 관리자 회원 관련 기능 -->

	<!-- 회원 전체 조회 -->
	<select id="memberList" resultType="admin.member.vo.MemberVO">
		select * from member
	</select>

	<!-- 회원 전체 조회2 : 탈퇴 회원 제외 전체 -->
	<!-- 회원 전체 조회3 : 탈되 회원만 조회 -->

	<!-- 회원 하나 조회 -->
	<select id="memberSelect" parameterType="admin.member.vo.MemberVO" resultType="admin.member.vo.MemberVO">
		select * from member where id = #{userID}
	</select>

	<!-- 회원 등록 -->
	<insert id = "memberInsert" parameterType="admin.member.vo.MemberVO">
		insert into member values 
		(#{userID}, #{password}, #{address}, #{email}, #{gender}, #{phone}, #{birth} )
	</insert>

	<!-- 회원 수정 -->
	<update id="memberUpdate" parameterType="admin.member.vo.MemberVO">
		update member 
		set password = #{password}, address = #{address}, 
						email = #{email}, gender = #{gender}, 
						phone = #{phone}, birth = #{birth} 
		where userID = #{userID}  
	</update>

	<!-- 회원 삭제 -->
	<update id="memberDelete" parameterType="admin.member.vo.MemberVO">
		update member 
		set password = #{password}, 
						address = #{address, jdbcType=VARCHAR}, 
						email = #{email, jdbcType=VARCHAR}, gender = #{gender, jdbcType=VARCHAR}, 
						phone = #{phone, jdbcType=VARCHAR}, birth = #{birth, jdbcType=VARCHAR} 
		where userID = #{userID}  
	</update>
</mapper>